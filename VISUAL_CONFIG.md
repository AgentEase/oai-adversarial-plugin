# AgentEase 可视化配置

CPA 插件管理 → O/对抗插件 → 配置字段。版本 `1.5.39`（与上游版本号对齐）
声明 25 个顶层字段，兼容 CPA 7.3.7 的字段编辑器。

额度耗尽和 HTTP 429 优先于 state 长度分类，分别显示“额度已用完”和“请求限流”。
路由暂时跳过冷却账号，选择宿主提供的健康候选；读取 Retry-After 或额度重置时间，
缺少时间时普通限流等待 60 秒、额度耗尽等待 5 分钟，到时重新参与选择。
旧客户端票据不能清除额度冷却，也不会通过探测尝试恢复额度。
没有可用替补时返回额度/限流的 429，而非“降智已拦截”。
宿主的候选范围、账号优先级和禁用设置继续生效，不越权启用账号。

票据到期显示“票据到期 · 待重探”，不作为账号降级或 401。
探测轨失败只记录为待重探，不覆盖仍有效的账号票据，也不单独创建业务降级拦截。
真实业务异常和鉴权失败仍保留保护，且账号级拒绝遵守“降智请求拒绝”总开关。

在账号路由和自动选号探测均启用时，每 30 秒扫描到期/待重探账号，无需业务
请求触发。任务按账号、模型与本次票据到期时间区分，不同账号同模型分别进入
串行队列，每份到期票据最多执行一次续期尝试；业务请求也可提前触发入队。
尝试标记在请求上游前持久化，保存失败不发请求，重启后不重复已尝试的票据。
排队中取消不消耗尝试；新票据有新的到期周期，执行前已续期的旧任务直接跳过。
失败后显示“续期已尝试 · 可手动重探”，不会自动循环重试。
到期扫描与“提前预备”窗口独立（窗口 0 只关闭提前预备），但遵守停止全部、
模型暂停、账号禁用、凭证冷却和探测关闭；不会修改固定账号或提取新代理。

推荐仅业务观测配置：

```yaml
operation-mode: business-only
override-policy: preserve-healthy-client
override-models: [gpt-6-astra]
experimental-account-routing: true
session-guard-mode: observe
session-provenance-ttl-minutes: 60
```

此模式不需要固定账号或静态 state，不会启动探测。没有有效基线时不注入；
真实业务流量观测到符合现有模型及 292/332 长度规则的 state 后存入基线。
长度规则是本地策略，不证明账号正常或模型能力。

会话守卫建议先使用 `observe`：已知异账号票据只记录、不剥离；来源未知的
客户端票据可以透传，但不会直接写入所选账号池。观察面板中的异账号计数后，
再切换为 `enforce`，此时已知异账号/异模型票据会被剥离；只有所选账号自身
仍有效的健康票据才会替换接管。来源映射只在内存中保存短指纹和脱敏账号信息，
默认 60 分钟，不保存完整 state。HTTP/SSE 可由成功响应确认归属；当前宿主 ABI
无法提供 WebSocket 握手响应头，所以 WebSocket 只支持请求侧隔离。

CPA 响应钩子缺少选号元数据时，通过宿主请求 ID 关联请求阶段确认的 AuthID；
关联仅保存在内存中，15 分钟过期，最多 4096 条。用量回调不能单独把未知或
外来票据写入账号池。已在 CPA 7.3.7 上验证请求头合并的删除兼容处理，
覆盖 HTTP/SSE 的跨账号、跨模型票据清理。上述检查验证隔离行为，不能证明
票据长度代表模型能力，也不能据此宣称已恢复降级账号。

`probe` 模式支持自动选号或显式凭证路径。启用能力不等于开始任务；
开始/停止仍在状态路由台控制。生产使用前配置已有代理，不能填写取 IP API。

`probe-account-mode: highest-priority` 通过 CPA 宿主回调选择未禁用、可用且
优先级数值最高的 Codex 账号。401/403、429、模型不一致和异常 state 会触发
有限冷却并在后续尝试切换候选；Token 只存在于单次请求内存且不写入日志。
插件每 10 秒读取一次账号摘要；最高优先级候选变化时只触发一轮探测，摘要
轮询本身不发送模型请求。首次启动仅建立快照，不额外探测。

字段包括运行模式、账号路由、会话守卫模式与来源 TTL、覆写策略、模型数组、凭证/代理文件路径、TTL、预备窗口、
串行间隔、超时、每出口和每轮预算、失败阈值与冷却时间。数组填写 JSON，
例如 `["gpt-6-astra"]`。表单不提供 Token、代理密码或静态 state 输入。

已设置的顶层字段优先于旧 `turn-state-override` YAML；删除字段后恢复继承。
旧环境的表单空值表示未设置别名，不代表底层配置为空。状态路由台的
`runtime-settings.json` 对预备窗口、间隔及出口设置仍有最终优先权。
降智拒绝开关继续在状态路由台管理；时区仍为原版固定 America/Los_Angeles。

本版本修复关闭探测且静态值为空时误报“覆写：配置错误”的校验。
升级后应同时检查 `/v0/management/plugins` 返回非空 `config_fields`、插件
摘要 `turn_state_override.error` 为空、探测关闭及业务观测记录，不能只检查版本号。
