# AgentEase 可视化配置

CPA 插件管理 → O/对抗插件 → 配置字段。由 AgentEase 提供初始实现，维护者追加兼容性与策略修复。
声明可编辑顶层字段，兼容 CPA 7.3.7 的字段编辑器。

推荐仅业务观测配置：

```yaml
operation-mode: business-only
override-policy: preserve-healthy-client
override-models: [gpt-6-astra]
```

此模式不需要固定账号或静态 state，不会启动探测。没有有效基线时不注入；
真实业务流量观测到模型一致、符合配置长度策略的 state 后存入基线。
长度规则是本地策略，不证明账号正常或模型能力。

`probe` 模式支持自动选号或显式凭证路径。启用能力不等于开始任务；
开始/停止仍在探测控制台控制。生产使用前配置已有代理，不能填写取 IP API。

`probe-account-mode: highest-priority` 通过 CPA 宿主回调选择未禁用、可用且
优先级数值最高的 Codex 账号。401/403、429、模型不一致和异常 state 会触发
有限冷却并在后续尝试切换候选；Token 只存在于单次请求内存且不写入日志。
插件每 10 秒读取一次账号摘要；最高优先级候选变化时只触发一轮探测，摘要
轮询本身不发送模型请求。首次启动仅建立快照，不额外探测。预备窗口为 0、全局停止或暂停时遵守原有门禁，不因账号切换恢复自动探测。

字段包括运行模式、覆写策略、模型数组、凭证/代理文件路径、TTL、预备窗口、
串行间隔、超时、每出口和每轮预算、失败阈值与冷却时间。数组填写 JSON，
例如 `["gpt-6-astra"]`。表单不提供 Token、代理密码或静态 state 输入。

已设置的顶层字段优先于旧 `turn-state-override` YAML；删除字段后恢复继承。
旧环境的表单空值表示未设置别名，不代表底层配置为空。探测控制台的
`runtime-settings.json` 对预备窗口、间隔及出口设置仍有最终优先权。
降智拒绝开关继续在探测控制台管理；时区仍为原版固定 America/Los_Angeles。

本版本修复关闭探测且静态值为空时误报“覆写：配置错误”的校验。
升级后应同时检查 `/v0/management/plugins` 返回非空 `config_fields`、插件
摘要 `turn_state_override.error` 为空、探测关闭及业务观测记录，不能只检查版本号。

## 可配置长度策略

默认兼容列表为 `[292, 332]`，这是可替换的经验默认值，不按 Team、个人或其他套餐名称自动判断。不同账号可能共用一个显式验收列表；应根据实际证据配置，长度匹配仍不证明账号或模型能力。

```yaml
accepted-state-lengths: [292, 332]
```

也可在 `turn-state-override.accepted-state-lengths` 中配置；顶层别名优先。接受 1–32 个不同整数，每个 1–4096，空数组、重复值、浮点数和字符串均拒绝。正常业务、客户端 State 保留、探测验收和账号路由共用该配置；管理页根据 API 策略显示，不另写固定长度规则。

账号健康证据最多有效一小时，普通成功但无 State 的请求不续期证据；同一规则用于运行和快照恢复。旧快照缺少健康证据时间时先按未知处理。成功请求重置连续失败计数，代理/传输错误及 429 不处罚账号；真正的 401/403 仍按认证异常处理。路由默认关闭，启用后也只在宿主提供的同优先级候选中选择。
